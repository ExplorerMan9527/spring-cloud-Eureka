server:
  port: 1112
spring:
  profiles:
    active: peer2

eureka:
  instance:
    hostname: peer2
    instance-id:  Peer2-Eureka-Server
  client:
    #fetch-registry: false
    #register-with-eureka: false
    service-url:
      defaultZone: http://peer1:1111/eureka/
